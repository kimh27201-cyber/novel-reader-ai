# 听读 2.0 本地角色音验收报告

## 交付范围

- 五种本地角色效果：萝莉、大叔、青年、正太、朗诵。
- 角色效果由设备系统 TTS 的音调与节奏组合生成。
- 支持 H5、HBuilderX App-Plus 和自建 Android WebView APK。
- 支持试听、停止、选择、持久化、阅读器使用和失效回退。
- 正文不上传，不接入云端 TTS，不新增依赖或后端接口。

## 自动验证

- 前端完整测试：`68 passed，0 failed`。
- 覆盖角色配置、音调和语速边界、三端驱动传参、播放中切换、偏好持久化、非法角色回退及 Android 桥静态契约。
- H5 生产编译：通过。
- Java 编译和 APK 构建：通过。
- APK v1、v2、v3 签名：通过。

## Chrome H5 验收

- 页面正确展示 5 张角色卡及各自音调、节奏参数。
- 角色试听后按钮切换为“停止”，再次点击可停止。
- 选择萝莉后显示“当前/已选择”。
- 刷新页面后仍保存 `ttsVoiceProvider=preset`、`ttsVoiceId=loli` 和 `ttsVoiceName=萝莉 · 本地角色`。
- 可在角色效果和系统音色之间切换。
- 390×844 移动端视口布局正常，角色卡可横向滚动。
- 浏览器控制台：`0 errors，0 warnings`。

## 手机1自建 APK 验收

- 设备：华为 `REA-AN00`，Android 15。
- APK 覆盖安装成功，原应用数据保留，应用启动成功。
- 声音选择页显示 5 种角色效果和 2 个本地中文系统声音。
- 选择萝莉后角色卡显示“当前/已选择”。
- 试听开始后按钮显示“停止”，证明 Android 原生 TTS 已接受朗读。
- 试听期间切到后台再返回，按钮恢复“试听”，声音会话已停止。
- 返回阅读器开始听读后，控制条显示“萝莉 · 本地角色”，当前段落高亮正常。
- 正式听读期间切到后台再返回，听读控制条消失，需要手动重新开始，符合前台播放边界。
- 验收期间 logcat 未发现 `FATAL EXCEPTION` 或 `AndroidRuntime` 崩溃。

## APK

- 文件：`D:\Codex\tts-role-apk-final-20260724\release\V2.apk`
- 大小：`1,348,282 bytes`
- SHA-256：`5E7ABEA1818E29F772358531D46B27C4014CE19C27CA01254845E9DD7375C12C`

## 验收结论

本地角色音的代码、构建、浏览器交互、Android 原生桥、选择持久化、正式听读和后台停止等客观验收项通过，可以交付。

“萝莉/大叔等角色听感是否符合个人偏好”属于主观听觉评价，自动化和 ADB 无法替代人耳评分；本报告仅确认五组参数确实不同且已传入三端语音驱动。
