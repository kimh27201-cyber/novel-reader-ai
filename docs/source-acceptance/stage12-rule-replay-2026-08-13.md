# 第十二阶段 YCK 规则失败重放（2026-08-13）

- 重放 12 个历史严格分母失败源；不保存正文、Cookie、Token、完整响应或完整书源 JSON。
- ID 6543 已由 `CONTENT_EMPTY` 提升为完整链路通过：搜索“斗破苍穹”、4718 章、抽样正文 8516 字符。
- ID 5992 已不再被请求模板解析器拒绝，进入真实请求后由站点返回 HTTP 500。
- ID 6248、7497 为 HTTP 403；ID 7406 超时；ID 7515 返回加载/访问壳。它们不通过扩大脚本权限绕过。
- ID 6752、6763、6618、6306 的返回结构与公开配置选择器不匹配，记录为站点变化或配置失效证据，不增加站点专用硬编码。
- ID 6438 暴露 `@title/@alt` 属性链差异，ID 7415 暴露 `:has/:contains` 声明式差异；两项已修复并加入本地夹具，受外部执行额度限制尚未再次联网重放。

安全边界测试同时覆盖成功样本、函数/原型字段拒绝、来源隔离和流程缓存预算超限。完整机器可读明细见 `stage12-rule-replay-2026-08-13.json`。
