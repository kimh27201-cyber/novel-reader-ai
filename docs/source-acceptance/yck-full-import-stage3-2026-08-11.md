# YCK 全目录导入验收报告（阶段 3）

验收时间：2026-08-11  
仓库：<https://www.yckceo.com/yuedu/shuyuan/index.html>  
原始结果：`yck-full-import-stage3-2026-08-11.json`

## 验收范围

本报告验证“YCK 当前目录中的合法书源配置能否完整下载、归一化、静态分类、去重并写入 Android 大容量本地存储”。它不把“配置已保存”等同于“站点可稳定阅读”。

- 顺序读取仓库全部 57 页。
- 每页优先使用 YCK 批量 JSON 接口，缺项时回退到单源 JSON。
- 按规范化名称与基础 URL 生成 `sourceKey`，覆盖重复配置。
- 只自动启用静态状态为 `ready` 且原配置允许启用的来源。
- 不保存第三方正文、Cookie、Token 或完整书源 JSON。

## 结果

| 指标 | 数量 |
|---|---:|
| 仓库声明条目 | 5621 |
| 实际目录条目 | 5621 |
| JSON 下载成功 | 5621 |
| 缺失 / 无效 | 0 / 0 |
| 唯一安装书源 | 5327 |
| 新增 / 覆盖 | 5327 / 294 |
| `sourceKey` 重复残留 | 0 |
| 自动启用 / 默认禁用 | 2356 / 2971 |
| 原生分片 / 存储字节 | 214 / 39,669,743 |

静态状态：`ready 2537`、`partial 700`、`blocked 1870`、`needs_login 220`。

来源类型：文字 5072、图片 49、音频 173、其他已声明类型 28、未声明 5。第一阶段只运行文字小说；其他类型可以导入和保留，但默认禁用。

唯一键集合 SHA-256：`ede7d0cd04ee60090f5777f9d425cabb3e56987c6d37d4acaf592e0f6550fa37`。

## 判定

- 全目录配置导入：**通过**。有效 JSON 导入率为 100%，高于 95% 目标。
- 大容量去重落盘：**通过**。5327 个唯一来源、重复残留 0。
- 全部来源稳定阅读：**未宣称通过**。搜索→详情→目录→正文的运行时通过率必须使用独立 200 源基准和 Android 真机复测，第 11 节记录的结果仍低于 80% 发布门槛。
- 访问控制：登录、验证码、付费及受限来源仅分类提示，不尝试绕过。

## 复现命令

在项目根目录执行：

```powershell
node scripts\yck_full_import_acceptance.mjs
```

脚本输出逐页累计进度，并生成不含原始配置和正文的 JSON 报告。
