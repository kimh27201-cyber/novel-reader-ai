# YCK 自适应运行池与 Android 真机验收（Stage 6）

## 范围与隐私

- 日期：2026-08-13
- 设备：REA-AN00，Android 15
- 本地书源：5330 条，覆盖安装和重启后保留
- 书源仓库：`https://www.yckceo.com/yuedu/shuyuan/index.html`
- 本报告不保存正文、Cookie、Token、完整响应或完整书源 JSON。

## 实现验收

- `runtimeV2` 分别跟踪搜索、发现、详情、目录和正文，状态为 `untested/probing/passed/cooldown/blocked`。
- 未测试但静态合格的文字源可自动进入搜索探测，不再要求手动逐个测试。
- Android 本地搜索始终启动；后端登录结果仅并行合并，后端超时或离线不取消本地搜索。
- 默认最多探测 20 个来源，并发 4，单源超时 6 秒，整轮上限 20 秒；无结果时可继续下一批。
- 发现分类最多跨 3 个提供者回退，并发最多 2 个；全部失败后返回稳定错误汇总。
- Wi-Fi 预热仅在应用前台且空闲时执行，移动数据、阅读、搜索和应用隐藏时暂停。

## 自动验证

| 项目 | 结果 |
|---|---:|
| 前端 `*.test.mjs` | 101 passed / 0 failed |
| 后端 SQLite | 125 passed / 0 failed |
| 固定 200 源合法导入 | 200 / 200（100%） |
| 静态候选 / 实际流程测试 | 36 / 36 |
| 严格运行时分母 | 3 |
| 完整阅读通过 | 1 / 3（33.33%） |

外部排除：`NETWORK_ERROR 13`、`TIMEOUT 6`、`HTTP_BLOCKED 6`、`SITE_UNREACHABLE 4`、`HTTP_NOT_FOUND 2`、`HTTP_SERVER_ERROR 2`。严格分母内另有 `SEARCH_EMPTY 2`。逐项 ID、SHA-256、耗时和错误码见同目录 `yck-text-source-benchmark-stage6-2026-08-13.json`。

## 真机观察

1. 5330 条数据下，优化前发现页初始化约 28 秒；移除重复配置解析、重复哈希和 O(n²) 查找后，页面框架约 4 秒可交互，完整分类目录约 10 秒出现。
2. 聚合发现先后抽样“网游”“都市”等入口，首源失败后继续尝试备用提供者；全部失败时显示“已尝试多个发现来源，暂时都不可用”，没有无限加载。
3. 搜索“斗破苍穹”在 20 秒内完成 20 个来源探测，本轮未获得结果；来源统计随失败冷却更新，失败卡片未混入图书结果。
4. 后续代码增加真实完整通过源的冷启动排序，并修复无结果页被发现目录遮挡的问题；这两项已通过自动测试并进入最终 APK，但尚待设备重新连接后复测。
5. FastAPI 健康检查返回 `ok`，ADB 保留 `tcp:8765` 供账号、同步和 AI 语音；书源本地运行链路不依赖该端口。

## 产物状态与结论

- 当前工作区候选 APK：`release/android-v2/V2.apk`
- 大小：6,260,746 字节
- SHA-256：`AEBDCC9D87F7E4CA2A3B022DBAC20371848D83B1F94B0295867727D6E5B3405A`
- H5 与 APK 最终构建成功，APK v1、v2、v3 签名通过。
- 覆盖安装前无线 ADB 断开，最终包尚未完成 REA-AN00 现场复测。

结论：本阶段已修复“可搜索 0”、后端替代本地搜索、发现无跨源回退和阶段状态不统一的代码根因；但真实严格分母和完整阅读率仍未达标。PR #1 应继续保持草稿，不宣称 YCK 全部或绝大来源已经稳定可读。

## 最终真机复测与热修

- REA-AN00 覆盖安装成功，5330 条书源、两本书架图书和阅读记录保留。
- 首轮无结果时发现“继续检测下一批”按钮事件未生效；改为搜索页原生按钮后，点击 1.5 秒内进入加载状态。
- 第二批探测结果：有结果 1、空结果 2、失败 17；打开后识别《斗破苍穹》、作者九支书竹、目录 1642 章，第一章正文成功加载。
- 搜索卡片的临时标题为“未命名小说”，正文残留 `chap_tp(); theme();`，均作为已知解析清洗问题保留。
- “玄幻”发现分类跨多个提供者回退，最终稳定提示不可用，没有无限加载。
- 移除 `tcp:8765` 映射后，本地结果在 12 秒内出现；测试完成后恢复映射。
- 五种 AI 音色均完成真实合成和手机播放，真实性刷新与缓存复跑通过。完整 11 项验收在 8 项通过后由操作人员主动停止长时连续播放。
- 最终 APK：6,433,142 字节；SHA-256：`D9FD511D04340AD0065A726BAEE4EDBD234467A4BCA155E000FEBC5BD1D9E1B8`；v1/v2/v3 签名通过并覆盖安装成功。
