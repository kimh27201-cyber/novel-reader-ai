# YCK Android 真机全目录导入验收（2026-08-12）

验收设备为 REA-AN00（Android 15），应用 `com.novelreader.v1`，版本 `1.0.0 (10000)`。电脑后端 8765 未启动，也没有 `adb reverse tcp:8765`；书源目录和 JSON 均由 APK 的 `NovelReaderHttp` 本地网络桥直接访问。

| 指标 | 结果 |
|---|---:|
| YCK 当时声明总数 | 5624 |
| 处理页数 | 57 / 57 |
| 已处理配置 | 5624 |
| 下载缺失 | 0 |
| 新增 / 覆盖 | 5330 / 294 |
| 重启后本地书源 | 5330 |

批量接口出现连接提前关闭时，客户端自动回退到 4 路单源下载；中途 DNS 瞬时失败时保存断点，网络恢复后从第 2 页继续，最终缺失归零。强制停止并重新启动 APK 后，书源页仍显示 5330 个来源，证明大容量原生分片存储和重启加载通过。

最终 APK 大小 1,520,718 字节，SHA-256 为 `3C8BA0EFD6BCFB781188CA28063612CB74A1EFEACAA64B7D23D0DE656B542967`，v1/v2/v3 签名验证通过。最终包覆盖安装后再次强制停止、重启，仍显示 5330 源。现场截图只保存在本机忽略目录，不提交可能包含用户设备状态的图片；结构化报告不保存正文、Cookie 或 Token。

该验收证明“无需电脑后端即可完整导入并持久化 YCK 目录”，不等于 5330 个来源全部能稳定读取。真实阅读能力仍以独立固定样本报告和稳定错误分类为准。
