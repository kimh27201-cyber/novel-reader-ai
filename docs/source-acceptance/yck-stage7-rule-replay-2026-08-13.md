# 第七阶段高频规则差异重放（2026-08-13）

## 范围

- 基线：`yck-current-cohort-stage7-window1-2026-08-13.json` 中的 `PARSE_EMPTY` 样本。
- 目的：区分可修复的引擎差异与站点页面/配置变化；本报告不是第二时间窗口，不参与双窗口发布门槛统计。
- 数据边界：只记录 YCK ID、阶段状态、章节数、清洗后正文长度和错误码；不保存正文、Cookie、Token、完整响应或完整书源 JSON。

## 新增兼容能力

1. 搜索结果省略 `bookUrl` 时，如果标题规则明确定位到 `<a>`，可从同一声明式规则安全推导 `href`。
2. 书源规则中的 `{{book.kind}}`、`{{chapter.*}}` 等上下文可在搜索、详情、目录、正文阶段稳定使用。
3. 目录入口解析为空时，仅回退尝试当前书籍详情 URL；不跨来源、不猜测站点路径。
4. 支持声明式规则末尾的受控 `@js:` 转换，以及以换行分隔的 `let/const/var` 安全脚本语句；仍受时间、语句数、递归深度和结果大小预算限制。
5. 相对 URL 请求模板可作为规则结果返回，不再被误判为 CSS 选择器。

## 真实重放结果

| YCK ID | 首窗口 | 修复后 | 目录 | 首章清洗字符 | 说明 |
|---|---|---|---:|---:|---|
| 6645 | `PARSE_EMPTY` | `PASSED` | 30 | 141 | CSS 选择规则差异 |
| 6931 | `PARSE_EMPTY` | `PASSED` | 20 | 13781 | 声明式解析差异 |
| 6808 | `PARSE_EMPTY` | `PASSED` | 1663 | 3505 | 搜索 URL 推导 + 目录回退 |
| 6305 | `PARSE_EMPTY` | `PASSED` | 1663 | 2852 | `book.kind` 上下文 + 多行受控 JS 请求模板 |

当前已确认 4 个首窗口 `PARSE_EMPTY` 样本完成搜索→详情→目录→正文闭环。其他样本仍需按相同原则逐项区分站点失效、配置过期和可复现引擎差异。

## 安全与回归

- 前端全量：`108 / 108 passed`。
- `eval`、`Function`、任意 Java 类、文件系统、浏览器全局对象、循环和外部模块继续被拒绝。
- 新增测试覆盖 URL 推导、目录回退、`book.kind` 模板、换行分隔脚本和越界拒绝。
- 本次不修改首窗口原始报告；第二窗口仍须在首窗口抓取时间至少 24 小时后使用锁定清单执行。
