# YCK Android 发现页运行态验收（第五轮）

- 验收时间：2026-08-12 23:17–23:22（Asia/Shanghai）
- 设备：REA-AN00，Android 15
- 应用：`com.novelreader.v1`，覆盖安装且保留 5330 条书源
- APK SHA-256：`E882F28DA3ACFB134862081E129096FD8515A5C6A3719CCC41B865CFFE987C35`
- 后端：8765 健康，ADB `tcp:8765` 映射保留用于云服务和 AI 语音；书源请求仍由 APK 本地运行时处理

## 验收结果

| 步骤 | 发现源 | 发现入口 | 结果 |
|---|---:|---:|---|
| 覆盖安装后进入发现页 | 1373 | 21853 | 新口径同时显示来源和入口数量 |
| 打开 `阅书小说网 / 玄幻小说` | 1372 | 21845 | `m.yueshu.org` DNS 失败，分类为 `SITE_UNREACHABLE`，隔离 1 源 / 8 入口 |
| 继续抽样发现入口 | 1371 | 21836 | 目标返回 HTTP 404，分类为 `HTTP_NOT_FOUND`，再隔离 1 源 / 9 入口 |
| 最终 APK 再次覆盖安装并加载完成 | 1371 | 21836 | 运行态与 5330 条书源持久化均保留 |

## 交互验收

- DNS 异常不再显示 Android 英文原文，提示为“书源站点域名无法访问，已从发现页暂时隔离；请换源或稍后重新检测”。
- 错误卡片按钮显示“查看其他可用入口”，失效来源不再参与后续发现聚合。
- 书源列表区分规则兼容性与站点运行状态，避免把 `compatible` 误解为站点当前可访问。

## 隐私与边界

本报告只记录设备型号、聚合计数、目标域名、HTTP/DNS 分类和 APK 哈希；未记录正文、Cookie、Token、完整响应或完整书源 JSON。一次失败只说明本次运行态不可用；第三方站点恢复后仍需重新检测。全目录可导入不等于全部来源可稳定阅读。
