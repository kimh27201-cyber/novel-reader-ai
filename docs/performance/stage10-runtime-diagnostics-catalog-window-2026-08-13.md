# 第十阶段：书源运行诊断与长目录固定窗口验收

日期：2026-08-13（Asia/Shanghai）  
设备：REA-AN00，Android 15  
数据：5330 个本地书源、3 本书架书籍；覆盖安装保留原数据

## 实现结果

- 书源轻量索引增加 `runtimeState`、结果数、稳定错误码、HTTP 状态、检测时间和冷却截止时间，不包含 `raw` 配置、正文、Cookie、Token 或完整响应。
- 书源页增加已验证、待检测、冷却中、受限快捷筛选，并显示前六类稳定失败码聚合。
- 阅读器目录改为固定 120 行窗口；999/1642 章一类长目录不再随滚动把全部章节克隆到 Vue 响应式列表。
- 目录根据实时 `scrollTop` 提前移动窗口，上下占位保持总滚动高度；真机发现并修复了仅在滚动到底时换窗导致的占位空白。

## 自动与构建验收

| 项目 | 结果 |
| --- | ---: |
| 前端 `*.test.mjs` | 114 / 114 passed |
| 后端 SQLite | 125 / 125 passed |
| H5 生产构建 | 通过 |
| APK 签名 | v1 / v2 / v3 通过 |
| `git diff --check` | 通过 |

H5 保留既有 Browserslist 数据过期和入口约 276KiB 警告；本阶段未新增运行时依赖或数据库表。

DOCX 渲染工具因环境仍缺少 LibreOffice/soffice 无法生成页面 PNG；已执行 ZIP 结构、`word/document.xml`、样式、关系、阶段标题、测试数量和 APK 哈希审计，结构检查通过。该限制不等同于逐页视觉检查通过。

## REA-AN00 验收

| 场景 | 结果 |
| --- | --- |
| 覆盖安装 | 成功；5330 源、3 本书架、阅读进度保留 |
| 运行诊断 | 已验证 0、待检测 5217、冷却中 100、受限 13、失败 113 |
| 失败聚合 | `PARSE_EMPTY 23`、`TIMEOUT 21`、`HTTP_NOT_FOUND/404 17`、`SITE_UNREACHABLE 17`、`NETWORK_ERROR 11`、`REQUEST_TEMPLATE_UNSUPPORTED 11` |
| 长目录 | 999 章书籍连续快速滚动 10 次，从第 1 章稳定到第 984 章，无空白、无崩溃 |
| 静置 PSS | 145,282KB；接近阶段九 142,817KB 稳态基线 |

## 最终 APK

- 路径：`release/android-stage10-runtime/V2.apk`
- 大小：1,360,350 字节
- SHA-256：`C285CE306891130CAD84C11BE48700C0AC7BEFAC8E6CDB9621A5FBFC3CF2860A`

## 边界

- 本阶段没有提前执行阶段七第二时间窗口，也没有改变冻结清单、书源解析规则或完整阅读率统计。
- 第二时间窗口最早仍为北京时间 2026-08-14 15:47；达标前 PR #1 继续保持 Draft。
- 运行诊断中的“失败”表示已有稳定错误码，不等于删除或停用来源；失败来源仍保留，按冷却和访问限制参与后续调度。
