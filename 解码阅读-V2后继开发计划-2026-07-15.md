# 解码阅读 V2 后继开发计划

> 基准日期：2026-07-15（距上次真机审查 2 天）
> 当前评分：**5.5/10**（审查基准日 2026-07-13）
> 目标：从功能完备走向设计精品
> 参考文档：[V2前端开发总计划](./解码阅读-V2前端开发总计划.md) · [审查报告](./V2前端实现审查报告-2026-07-13.md)

---

## 当前状态回顾

### 已完成（Codex 工作成果）

| 维度 | 状态 |
|------|------|
| 自定义 Tab 栏 + 标签文字 | ✅ 已上线，4 个 Tab 带标签和主题样式 |
| 主题 Token 体系 | ✅ 9 个主题定义，5 个激活（玄夜/糖果/樱雾/量子/黑曜） |
| App.vue 主题 CSS | ✅ 690 行全局样式，五大主题差异化规则 |
| 页面功能 | ✅ 14 个页面功能完备（书架/书源/发现/我的/阅读器等） |
| 间距/字体/动效 Token | ✅ `--app-space-*`、`--app-font-size-*`、`--app-motion-*` 已定义 |
| 指示器 spring 曲线 | ✅ 已升级到 `cubic-bezier(0.34, 1.56, 0.64, 1)` 380ms |
| :active 即时反馈 | ✅ 图标 scale(0.85) 60ms |

### 核心差距（从审查报告提炼）

| # | 维度 | 当前评分 | 目标 | 关键问题 |
|---|------|---------|------|---------|
| 1 | 去 AI 味 | 4/10 | 9/10 | 玻璃拟物仍重、卡片千篇一律、字体无性格 |
| 2 | 主题差异化 | 5/10 | 9/10 | 仅换了颜色，材质/间距/装饰未深入 |
| 3 | 交互动效 | 6/10 | 9/10 | 指示器只有位移动画，无 stagger、无手势滑动 |
| 4 | 信息架构 | 5/10 | 8/10 | DEBUG MODE、技术术语直接暴露给用户 |

---

## 第一阶段：去 AI 味 + 收口（2-3 天）🔴

审查报告中"低成本高影响"的具体修改，每一项都能在 1 小时内完成。

### 1.1 隐藏开发者工具

| 文件 | 改动 | 预估 |
|------|------|------|
| `pages/profile/profile.vue` | DEBUG MODE 默认折叠，仅连续点击版本号 7 次显示 | 30min |
| `pages/profile/profile.vue` | 后端连接卡片默认折叠为一行摘要，点击展开 | 1h |
| `pages/library/library.vue` | 管理工具区域（批量检测/健康评分/验收）默认折叠 | 30min |
| `pages/search/search.vue` | 高级设置（并发/超时/源数调节器）默认折叠 | 30min |

**预期效果**：让阅读 app 看起来像阅读 app，不是开发者后台。

### 1.2 玻璃拟物收敛

| 文件 | 改动 |
|------|------|
| `App.vue` 全局 | 面板/卡片/弹层 `backdrop-filter` → 实色背景；仅导航栏保留玻璃效果 |
| `App.vue` 全局 | 弹层 backdrop-filter blur(10px) → blur(6px)；亮色主题（糖果/樱雾）使用实色背景 |
| `pages/bookshelf/bookshelf.vue` | 更多菜单：`rgba(33,34,33,0.98)` 纯色暗块 → 毛玻璃+主题色边框 |

**预期效果**：减少"AI 生成 UI"的特征指纹。玻璃拟物是 AI 生成界面的默认选择，收敛后人工设计感立刻提升。

### 1.3 硬编码颜色消除

全项目搜索以下硬编码值，全部替换为对应的 CSS 变量：

```
#60757d → var(--app-top)
#2b2c2b → var(--app-panel-strong) 或对应主题 token
#20211f → var(--app-stage)
```

影响页面：`bookshelf.vue`（顶部背景）、`search/search.vue`、`profile/profile.vue`。

### 1.4 文案去技术化

| 当前文案 | 改为 |
|---------|------|
| "自检" / "批量检测" | "检查书源是否可用" |
| "验收" | "确认导入" |
| "健康评分" | "书源状态" |
| "并发数" | 隐藏到高级设置 |
| "v1.0.0 (build 10000)" | "解码阅读 V1" |

### 1.5 Tab 栏图标三段式弹簧

```
当前：active 态 scale(1.08) + translateY(-2rpx)
目标：触碰瞬间 scale(0.9) → 释放 scale(1.12) → 回弹 scale(1.08)
     过渡时间 280ms，cubic-bezier(0.34, 1.56, 0.64, 1)
```

这是用户最频繁触碰的控件，每一点提升都会被高频感知。

---

## 第二阶段：动效体系（3-4 天）🟡

### 2.1 Stagger 列表入场动画

- **书架页**：每本书卡片从下往上交错淡入，stagger 60ms
- **书源页**：已安装书源列表同样 stagger 入场
- **实现方案**：Vue transition-group + 动态 animation-delay

```css
.shelf-stagger-enter {
  opacity: 0;
  transform: translateY(20rpx);
}
.shelf-stagger-enter-active {
  transition: opacity 280ms ease, transform 320ms cubic-bezier(0.34, 1.56, 0.64, 1);
}
/* animation-delay 通过 :style 动态注入 */
```

### 2.2 弹层动画

| 弹层类型 | 入场 | 退出 |
|---------|------|------|
| 底部 Sheet | 从底部滑入 `cubic-bezier(0.32, 0.72, 0, 1)` 300ms | 向下滑出 200ms |
| 遮罩层 | opacity 0→1 200ms | opacity 1→0 150ms |
| 居中 Modal | scale(0.92)→scale(1) spring 320ms | scale(0.95)+opacity 0 180ms |

### 2.3 骨架屏系统

| 页面 | 骨架内容 | 动画 |
|------|---------|------|
| 书架 | 4-6 个矩形块（模拟封面+标题行） | shimmer |
| 发现/搜索 | 搜索框 + 3 行结果占位 | shimmer |
| 书源 | 列表行占位（图标+3行文字） | shimmer |
| 我的 | 设置项占位（图标+文字行） | shimmer |

Shimmer 动画实现：

```css
.skeleton {
  background: linear-gradient(
    90deg,
    var(--app-panel) 25%,
    var(--app-panel-strong) 50%,
    var(--app-panel) 75%
  );
  background-size: 200% 100%;
  animation: shimmer 1.4s ease-in-out infinite;
}

@keyframes shimmer {
  from { background-position: 200% 0; }
  to { background-position: -200% 0; }
}
```

### 2.4 页面过渡转场

| 转场类型 | 动画 | 时长 |
|---------|------|------|
| push（进入子页） | 新页面从右滑入 | 300ms |
| pop（返回） | 当前页面向右滑出 | 250ms |
| tab 切换 | 内容区淡入 | 200ms |

---

## 第三阶段：主题深度差异化（5-7 天）🟢

这是从 5.5 分跃升到 8+ 分的关键阶段。每个主题要实现的不只是颜色变化，而是材质/字体/间距/装饰元素的系统性差异。

### 3.1 玄夜（Xuanye）— 深夜黑客

**当前状态**：颜色 token 已定义，App.vue 中有主题 CSS 规则
**需补充**：

| 元素 | 实现方式 |
|------|---------|
| 书脊发光竖线 | 书架列表项左侧 3rpx 宽 `var(--app-accent)` 竖线 + `box-shadow` 发光 |
| 终端风空状态 | 黑底 + `█` 闪烁光标 CSS animation（blink 1s step-end infinite） |
| 荧光分隔线 | 顶部区域底部 `linear-gradient(90deg, transparent, var(--app-accent), transparent)` 分隔线 |
| 代码风格标签 | 章节数/状态标签使用 `--app-utility-font`（Cascadia Mono） |

### 3.2 糖果绘本（Candy）— 快乐书虫

**当前状态**：颜色+粗黑描边已定义，有部分 CSS 规则
**需补充**：

| 元素 | 实现方式 |
|------|---------|
| 封面贴纸角标 | `::after` 伪元素彩色圆形，`top: -7rpx; right: -7rpx`，`border: 2rpx solid rgba(52,42,50,0.78)` |
| 卡片随机微旋转 | `:nth-child(3n+1) { transform: rotate(-0.55deg) }` 已有，需确保全页面生效 |
| 印章感按钮 | `border: 2rpx solid rgba(52,42,50,0.78)` + 偏移 `box-shadow: 4rpx 5rpx 0` |
| 手写点缀 | 书名使用 KaiTi/STKaiti |

### 3.3 樱雾少女（Sakura）— 文艺少女

**当前状态**：颜色+丝带装饰 CSS 规则已定义
**需补充**：

| 元素 | 实现方式 |
|------|---------|
| 丝绸高光 | 卡片 `::before` 伪元素，`linear-gradient(120deg, transparent 44%, rgba(255,255,255,0.66) 45% 52%, transparent 53%)` |
| 柔和扩散光 | 卡片选中态 `box-shadow: 0 0 34rpx rgba(233,122,174,0.24)` 替代硬阴影 |
| 空状态花瓣 | CSS 伪元素生成粉色半透明圆点，多组分散布局 |
| 渐变按钮 | `background: linear-gradient(100deg, var(--app-accent), var(--app-accent-2))` |

### 3.4 量子蓝图（Cyber）— 数据探员

**当前状态**：颜色+网格背景已定义
**需补充**：

| 元素 | 实现方式 |
|------|---------|
| HUD 十字准星空状态 | 圆形+十字线 CSS 绘制 |
| 分段式进度条 | `repeating-linear-gradient(90deg, var(--app-accent) 0 18rpx, var(--app-accent-3) 18rpx 22rpx, transparent 22rpx 28rpx)` |
| 扫描线动画数据标签 | `background: repeating-linear-gradient(0deg, transparent, transparent 2rpx, rgba(52,214,255,0.1) 2rpx, rgba(52,214,255,0.1) 4rpx)` + animation |
| 等宽字体全页面 | `--app-display-font` 和 `--app-utility-font` 均为 Cascadia Mono |

### 3.5 黑曜金（Noir Gold）— 藏书家

**当前状态**：颜色+金线边框已定义
**需补充**：

| 元素 | 实现方式 |
|------|---------|
| 藏书票角标 | 卡片 `::before` 四个角 L 形金线（`border-top` + `border-left` on ::before, `border-bottom` + `border-right` on ::after） |
| 皮革纹理 | `background-image` 使用细微噪点渐变模拟皮革 |
| 宋体全页面 | `--app-display-font: "Songti SC", "STSong", serif`，确保 title/screen-title 全部使用 |
| 金色渐变进度条 | `linear-gradient(90deg, #8c6a32, var(--app-accent), #f2e2b5)` |

### 3.6 空状态主题化

当前书架空状态是统一的虚线边框 → 每个主题独特插图：

```
玄夜：星轨图案 + "准备解码" 终端风
糖果：贴纸风 + "快来收藏第一本绘本吧!"
樱雾：飘落花瓣 + "翻开你的樱色故事"
量子：十字准星 + "AWAITING_DATA_INPUT"
黑曜：藏书票边框 + "你的私人图书馆等待第一本藏书"
```

---

## 第四阶段：信息架构重组（2-3 天）🟡

### 4.1 卡片变体系统

当前问题：所有 panel/card/row 都是 `border + border-radius + shadow` 三件套，没有信息层级。

**引入层级容器**：

```
信息密集区 → 分割线分隔（替代卡片包围）
列表项   → 左侧色条 + 背景色区分（替代边框卡片）
操作区   → 主题色边框 + 大间距（明确可点击）
元数据   → 缩进 + 小字号（降低视觉权重）
```

### 4.2 导入 Hub 重设计

**当前**：4 个小按钮挤在一行
**改为**：2×2 大卡片布局

```
┌──────────────────┐  ┌──────────────────┐
│  🔗 链接导入      │  │  📋 JSON 导入     │
│  粘贴书源 URL     │  │  粘贴 JSON 内容   │
└──────────────────┘  └──────────────────┘
┌──────────────────┐  ┌──────────────────┐
│  📷 扫码导入      │  │  📁 文件导入      │
│  扫描二维码添加   │  │  选择本地文件     │
└──────────────────┘  └──────────────────┘
```

每张卡片含图标+标题+副标题描述，主题色边框。

---

## 第五阶段：打磨 + 回归（2-3 天）🔵

### 5.1 图标系统 SVG 化

**当前**：使用 PNG 图片图标（normal/active 两套）
**目标**：iconfont 或内联 SVG

- 每个 Tab 图标：line（线性/未选中）→ fill（填充/选中）的切换动画
- 图标颜色跟随 `--app-accent`
- 减少包体积约 80KB

### 5.2 Tab 手势滑动导航

- 左右滑动切换 Tab（滑动超过 30% 屏幕宽度触发）
- 指示器实时跟手
- 与页面内部横向滚动不冲突（仅在 scrollLeft === 0 或 max 时触发）

### 5.3 下拉刷新

- 书架页 pull-to-refresh
- 搜索页 pull-to-refresh
- 主题化刷新动画

### 5.4 回归测试清单

- [ ] 书源导入（URL/JSON/扫码/文件）正常
- [ ] 搜索 → 详情 → 目录 → 正文 → 加入书架 全链路正常
- [ ] 书架左滑删除正常
- [ ] 主题切换持久化正常
- [ ] 五大主题 25 张截图（5 页面 × 5 主题）无明显 bug

---

## 实施路线图总览

```
Week 1 (7/15 — 7/21)  🔴 去 AI 味 + 收口
├── 隐藏 DEBUG MODE / 管理工具 / 高级设置        [30min×4 = 2h]
├── 玻璃拟物收敛（仅导航栏保留）                  [2h]
├── 硬编码颜色 → CSS 变量                         [1h]
├── 文案去技术化                                  [1h]
├── Tab 图标三段式弹簧                            [1h]
└── 顶部背景色主题化                              [1h]
                                                ≈ 1 天

Week 2 (7/22 — 7/28)  🟡 动效 + 信息架构
├── 书架/书源 stagger 入场动画                    [2h]
├── 弹层进出动画                                  [3h]
├── 骨架屏（书架+搜索+书源+我的）                 [3h]
├── 导入 Hub 2×2 卡片重布局                       [2h]
├── 页面过渡转场                                  [3h]
├── 卡片变体系统                                  [2h]
└── 按钮 :active 反馈增强                          [2h]
                                                ≈ 2-3 天

Week 3 (7/29 — 8/4)   🟢 主题深度差异化
├── 玄夜完整落地（书脊/终端风/荧光）              [1.5d]
├── 糖果绘本完整落地（贴纸/旋转/印章）            [1.5d]
├── 樱雾少女完整落地（高光/花瓣/扩散光）          [1.5d]
├── 量子蓝图完整落地（HUD/扫描线/分段进度）       [1.5d]
├── 黑曜金完整落地（藏书票/皮革/宋体）            [1.5d]
└── 空状态主题化                                  [1d]
                                                ≈ 5-6 天

Week 4 (8/5 — 8/11)   🔵 打磨 + 回归
├── 图标系统 SVG 化                               [4h]
├── Tab 手势滑动导航                              [4h]
├── 下拉刷新                                      [2h]
├── 五大主题全页面截图验收                        [2h]
├── Android 真机 60fps 回归                       [2h]
└── 功能全链路回归测试                            [2h]
                                                ≈ 2-3 天
```

---

## 不做的事（约束重申）

- ❌ 不迁移框架（保持 uni-app + Vue 2）
- ❌ 不重写后端接口
- ❌ 不改变现有页面路由结构
- ❌ 不修改书源解析 / 阅读进度 / 缓存逻辑
- ❌ 不引入新的 npm 依赖（除非绝对必要且轻量）
- ❌ 不改变 pages.json 中的 Tab 配置

---

## 验收标准

### 视觉验收

- [ ] 五个主题各截取书架/书源/发现/我的/阅读器 5 个页面共 25 张截图
- [ ] 每个主题的截图放在一起能明显看出设计风格差异（不只是颜色）
- [ ] 没有玻璃拟物 + 紫色渐变 + 等宽字体 + 圆角卡片的 AI 四件套痕迹
- [ ] 375px / 393px / 412px 三个视口无溢出、不错位

### 动效验收

- [ ] Tab 切换：指示器弹性滑动 + 图标三段式弹簧（录屏 30fps 无掉帧）
- [ ] 按钮点击：触碰瞬间有视觉反馈（50ms 内可见）
- [ ] 列表滚动：60fps 无卡顿（Chrome DevTools Performance 面板确认）
- [ ] 页面转场：流畅无白屏
- [ ] 列表入场：stagger 交错动画流畅

### 性能验收

- [ ] Lighthouse Performance 评分 > 85
- [ ] 首屏加载 < 1s（H5 模式）
- [ ] Tab 切换延迟 < 50ms
- [ ] 主题切换延迟 < 100ms
- [ ] Android 真机（荣耀 REA-AN00）60fps 滚动

### 功能回归

- [ ] 书源导入（URL/JSON/扫码/文件）正常
- [ ] 搜索 → 详情 → 目录 → 正文 → 加入书架 全链路正常
- [ ] 书架左滑删除正常
- [ ] 主题切换持久化正常
- [ ] 静态回归测试全部通过

---

## 参考

- [V2 前端开发总计划](./解码阅读-V2前端开发总计划.md) — 设计原则、五大主题规范、完整 Token 体系
- [V2 前端实现审查报告（2026-07-13）](./V2前端实现审查报告-2026-07-13.md) — Android 真机逐页审查
- [DESIGN.md](./DESIGN.md) — 项目设计文档
- [docs/SCREENSHOTS.md](./docs/SCREENSHOTS.md) — 截图参考

---

> **核心信念**：不要让人一眼看出"这是 AI 生成的界面"。
> 每个主题应该像一位有品味的设计师花了一周打磨出来的、有温度、有细节、有性格的作品。
> 少即是多，克制是最高级的表达。
