# 🎨 解码阅读 V2 前端体验升级 — 总计划

> 定位：让 Codex 实施的 V2 前端改造完整路线图
> 原则：去 AI 味、高质感、主题差异化、响应零延迟
> 技术栈：uni-app + Vue 2（不迁移框架）
> 创建日期：2026-07-13

---

## 〇、现状诊断：当前前端的 "AI 味" 来源

在深入分析了 16 张项目截图和全部前端源码后，以下是需要解决的核心问题：

| # | 问题 | 表现 | 根因 |
|---|------|------|------|
| 1 | **玻璃拟物泛滥** | 导航栏、面板、弹层全部使用 backdrop-filter blur，缺乏层次区分 | Glass-morphism 是 AI 生成 UI 的默认选择，缺少人工设计判断 |
| 2 | **卡片千篇一律** | 书架行、书源行、设置项都是同一个 card 模式（border + radius + shadow），没有信息层级 | 缺乏组件变体设计 |
| 3 | **字体缺乏性格** | 全局 system-ui 栈，虽有 KaiTi 混用但不系统化 | 主题字体定义存在但落实不彻底 |
| 4 | **色彩过于安全** | 每个主题仅靠 3-4 个 accent token 区分，缺乏中间色调和氛围层 | Token 体系不够细粒度 |
| 5 | **动效生硬** | Tab 指示器是简单 translate 跳变，页面切换无转场，列表无交错动画 | 缺乏动效体系和缓动曲线设计 |
| 6 | **图标语言陈旧** | 使用 emoji/unicode 字符（⌕、⋮、ⓘ）代替图标，在不同平台渲染不一致 | 缺乏统一图标系统 |
| 7 | **间距缺乏节奏** | padding/margin/gap 数值随意（28rpx、18rpx、14rpx 混用），没有网格基准 | 缺乏 spacing scale |
| 8 | **暗色主题偷懒** | 暗色主题（玄夜/黑曜金/量子蓝图）本质上是同一套暗色布局换色 | 主题差异化仅停留在 token 层面 |

---

## 一、设计总纲：五项核心原则

### 1.1 去 AI 味原则

> **不做**：玻璃拟物 + 圆角卡片 + 紫色渐变 + 等宽字体 = AI 四件套
> **要做**：每个主题像一个人工设计师精心打磨的品牌产品

具体约束：
- 玻璃效果（backdrop-filter）仅限导航栏使用，面板/弹层/卡片一律用实色背景
- 圆角使用要有意图：直角代表数据严谨，微圆角（4-8rpx）代表工具感，大圆角（18-26rpx）代表休闲/阅读感
- 每个主题的间距、字体、圆角必须有区别，不能只是颜色主题
- 不允许出现紫色到青色的渐变（AI 最爱），除非主题本身定义如此

### 1.2 响应零延迟原则

> 任何可交互元素从手指触碰到视觉反馈不超过 **50ms**

具体措施：
- 所有按钮使用 `:active` 伪类立即响应，不依赖 JS 事件
- 页面切换使用 `preload` + CSS transition，避免白屏
- 列表滚动使用 `will-change: transform` 和硬件加速层
- 图片使用 `lazy-load` + 占位色块，避免布局抖动
- Tab 切换使用 `requestAnimationFrame` 驱动的指示器动画

### 1.3 主题即人格原则

> 五个主题不是五个颜色方案，而是五种阅读人格

| 主题 | 人格 | 设计语言 | 代表材质 | 动效性格 |
|------|------|---------|---------|---------|
| 玄夜 | 深夜黑客 | 锐利、发光、代码感 | 荧光玻璃 + 终端 | 快、精准、弹簧 |
| 糖果绘本 | 快乐书虫 | 贴纸、印章、手账 | 和纸 + 糖果色块 | 弹跳、俏皮 |
| 樱雾少女 | 文艺少女 | 丝带、薄雾、花卉 | 雾面玻璃 + 丝绸 | 柔缓、飘逸 |
| 量子蓝图 | 数据探员 | 网格、扫描线、HUD | 全息玻璃 + 金属 | 机械、线性 |
| 黑曜金 | 藏书家 | 烫金、皮革、书脊 | 黑石 + 金箔 | 沉稳、重量感 |

### 1.4 信息层级的呼吸感

> 不是所有信息同等重要，UI 应该引导视线

- **一级信息**（书名、章节内容）：最大字号、最强对比、最多留白
- **二级信息**（作者、进度）：中等字号、中等对比
- **三级信息**（元数据、标签）：小字号、低对比、可被忽略
- **零级信息**（CTA 按钮）：主题色、高对比、唯一焦点

### 1.5 动效叙事原则

> 动效不是为了炫技，是为了告诉用户发生了什么

- **入场动画**：列表项从下往上交错淡入（stagger 50ms），暗示"这里有新内容"
- **退出动画**：元素缩小 + 淡出（150ms），暗示"内容被移除"
- **状态切换**：弹性缩放（spring），暗示"状态改变了"
- **导航切换**：指示器滑动 + 图标变形，暗示"你到了新地方"
- **滚动反馈**：顶部下拉弹性，暗示"已到边界"

---

## 二、底部导航栏 — 全面重构

> 这是用户最频繁操作的控件，必须做到零延迟、高质感、主题差异化

### 2.1 动画系统重构

**现状**：指示器 `translate3d(activeIndex * 100%, 0, 0)` + `transition: transform 240ms cubic-bezier(0.2, 0.8, 0.2, 1)`
**问题**：只有位移动画，没有弹性/惯性/形变，交互感弱

**目标方案**：

```
┌────────────────────────────────────────────────────┐
│  ┌──────┐  ┌──────┐  ┌──────┐  ┌──────┐          │
│  │ 书架 │  │ 书源 │  │ 发现 │  │ 我的 │          │
│  │  ⌂   │  │  ⊕   │  │  ⌕   │  │  ◉   │          │
│  └──────┘  └──────┘  └──────┘  └──────┘          │
│  ████████                                      │
│  ↑ 指示器（滑动 + 弹性变形）                    │
└────────────────────────────────────────────────────┘
```

**动画分层**：

1. **指示器滑动**（从当前位置到目标位置的平滑过渡）
   - 使用 CSS `cubic-bezier(0.34, 1.56, 0.64, 1)` → 弹性过冲效果
   - 持续时间：320ms（给弹簧足够时间回弹）
   - 到达目标后轻微回弹（overshoot 6-10%，取决于主题）

2. **图标变形**（选中态和默认态的平滑切换）
   - 选中图标：填充色渐变 + scale(1.15) + 微旋转
   - 未选中图标：scale(1.0) + 降低饱和度
   - 过渡时间：200ms，跟随指示器但略微延迟 30ms

3. **触觉波纹**（用户手指触碰瞬间的即时反馈）
   - 触碰瞬间：图标 scale(0.85)，持续 80ms
   - 松手后：scale(0.85) → scale(1.05) → scale(1.0)，spring 回弹
   - 这是零延迟的核心：纯 CSS `:active` 状态，不经过 JS

4. **滑动跟随**（用户横向滑动页面时指示器实时跟手）
   - 监听页面切换的滑动进度（需要 uni-app 页面栈协助）
   - 指示器宽度在滑动过程中先压缩再拉伸（果冻效果）
   - 这个功能支持左右滑动手势切换 Tab

### 2.2 图标系统升级

**现状**：使用 PNG 图片图标，需要 active/normal 两套
**目标**：使用 SVG 图标字体或内联 SVG，支持主题色和动画

**实现方案**（uni-app 兼容）：
- 使用 `iconfont` 或内联 SVG path
- 每个图标有两个状态：line（线性/未选中）和 fill（填充/选中）
- 图标切换动画：line → fill 的路径变形（使用 CSS clip-path 过渡模拟）

**图标设计规范**：

| Tab | 图标形状 | 含义 |
|-----|---------|------|
| 书架 | 书脊排列 | 藏书之处 |
| 书源 | 节点连接 | 源头活水 |
| 发现 | 放大镜/雷达 | 探索发现 |
| 我的 | 书签/印章 | 个人印记 |

### 2.3 主题差异化

每个主题的导航栏有不同的材质和动效性格：

| 主题 | 底栏材质 | 指示器形状 | 动效曲线 | 特殊效果 |
|------|---------|-----------|---------|---------|
| 玄夜 | 深色玻璃 + 青色发光边缘 | 细长发光条 | spring(1.5) | 指示器带呼吸光晕 |
| 糖果 | 白色厚卡纸 + 粗黑边框 | 圆角矩形 + 投影 | bounce | 指示器有贴纸翘边阴影 |
| 樱雾 | 雾面粉白 + 细玫瑰金边 | 椭圆胶囊 | smooth | 指示器有丝绸高光 |
| 量子 | 暗色金属 + 蓝色扫描线 | 直角矩形 | linear | 指示器带扫描线动画 |
| 黑曜 | 黑色大理石 + 金线边框 | 细金线 | heavy | 指示器是金色光带 |

### 2.4 手势导航支持

- **左右滑动切换 Tab**：用户在任意 Tab 页面向左/右滑动超过 30% 屏幕宽度时切换 Tab
- **跟手滑动**：指示器实时跟随手指位置
- **释放判定**：滑动超过阈值 → 切换；未超过 → 回弹
- **与页面内部横向滚动不冲突**：仅在页面边缘（scrollLeft === 0 或 max）时触发

---

## 三、五大主题深度设计规范

### 3.1 玄夜（Xuanye）— 深夜黑客

**设计关键词**：锐利 · 发光 · 代码感 · 高对比

```
色彩氛围：
  底色：深空黑 #080b10 → #121017
  主色：荧光青 #67fff2（hover 时发光增强）
  辅助：电光紫 #8f6dff
  警示：暖金 #d8a75f（用于关键标记，非大面积）

间距体系（tight）：
  基础网格：4rpx
  紧凑间距：12rpx（列表项内）
  标准间距：20rpx（列表项间）
  宽阔间距：32rpx（区块间）

字体体系：
  标题：DIN Alternate / "PingFang SC"（几何感无衬线）
  正文：system-ui（保持可读性）
  数据标签：Cascadia Mono / Consolas（等宽代码感）

圆角体系（sharp）：
  卡片：4rpx（接近直角）
  按钮：2rpx
  封面：0rpx
  标签：2rpx

特殊 UI 元素：
  - 顶部区域底部有一条青色发光分隔线（渐变淡出）
  - 书架列表项左侧有 3rpx 宽的青色发光竖线（书脊）
  - 进度条：流动的青色渐变（neonFlow 动画）
  - 选中态：青色外发光 box-shadow
  - 空状态：终端风格的 █ 闪烁光标

去除 AI 味改动：
  ✗ 取消大面积 backdrop-filter
  ✗ 取消圆角卡片
  ✓ 改为直角 + 细线分隔
  ✓ 代码风格的等宽字体数据标签
  ✓ 发光效果仅用于关键交互元素
```

### 3.2 糖果绘本（Candy）— 快乐书虫

**设计关键词**：贴纸 · 印章 · 手账 · 物理感

```
色彩氛围：
  底色：暖奶黄 #fff7d6 → 淡天蓝 #eafaff
  主色：珊瑚橙 #ff7a59
  辅助：天空蓝 #55c7e8
  点缀：蜡笔黄 #ffd34e

间距体系（loose）：
  基础网格：6rpx
  紧凑间距：16rpx
  标准间距：28rpx
  宽阔间距：44rpx

字体体系：
  标题：Arial Rounded MT Bold（圆体，像绘本标题）
  正文：system-ui
  手写点缀：KaiTi / STKaiti（用于书名）

圆角体系（round）：
  卡片：26rpx（大圆角）
  按钮：999rpx（胶囊）
  封面：20rpx
  标签：999rpx

特殊 UI 元素：
  - 封面图带有 2rpx 黑色描边 + 右下偏移投影（贴纸感）
  - 封面图右上角有一个彩色圆形装饰（像贴纸角标）
  - 每个书架卡片有轻微随机旋转（±0.5deg），手工排列感
  - 按钮有 2rpx 黑色描边 + 厚投影（印章感）
  - 选中态：scale(1.02) + 加深投影

去除 AI 味改动：
  ✗ 取消玻璃效果
  ✗ 取消标准灰色边框
  ✓ 改为粗黑描边 + 彩色投影
  ✓ 封面加入贴纸角标
  ✓ 卡片随机微旋转
  ✓ 按钮像真实印章
```

### 3.3 樱雾少女（Sakura）— 文艺少女

**设计关键词**：丝带 · 薄雾 · 花卉 · 精致

```
色彩氛围：
  底色：樱粉白 #fff6fa → 淡紫 #f6f1ff
  主色：玫瑰粉 #d9609a
  辅助：薰衣草紫 #8e73d8
  点缀：樱花粉 #f4b9cb

间距体系（airy）：
  基础网格：6rpx
  紧凑间距：20rpx
  标准间距：32rpx
  宽阔间距：48rpx

字体体系：
  标题：STKaiti / KaiTi（楷体，文艺感）
  正文：PingFang SC（保持可读）
  装饰：Georgia / "Times New Roman"（衬线点缀）

圆角体系（soft）：
  卡片：24rpx
  按钮：999rpx（完全圆角）
  封面：18rpx
  标签：999rpx

特殊 UI 元素：
  - 卡片顶部有细丝带装饰（伪元素渐变条）
  - 选中态有柔和粉光（box-shadow spread）
  - 滚动条自定义为粉色细条
  - 空状态装饰为飘落花瓣图案
  - 渐变按钮：粉→紫

去除 AI 味改动：
  ✗ 取消通用阴影
  ✗ 取消等宽字体
  ✓ 丝带装饰替代分割线
  ✓ 楷体标题家族
  ✓ 柔和扩散光替代硬阴影
```

### 3.4 量子蓝图（Cyber）— 数据探员

**设计关键词**：网格 · 扫描线 · HUD · 精密

```
色彩氛围：
  底色：深蓝黑 #050b18 → #050914
  主色：冷光蓝 #34d6ff
  辅助：电光紫 #6d7cff
  点缀：荧光绿 #a7ff5e

间距体系（compact）：
  基础网格：4rpx
  紧凑间距：12rpx
  标准间距：18rpx
  宽阔间距：28rpx

字体体系：
  标题：Cascadia Mono / Consolas（等宽，像终端输出）
  正文：system-ui
  标签：Cascadia Mono

圆角体系（none）：
  卡片：0rpx（完全直角）
  按钮：2rpx
  封面：0rpx
  标签：0rpx

特殊 UI 元素：
  - 全局细网格背景（28rpx 间距的青色网格线）
  - 数据标签带扫描线动画（repeating-linear-gradient）
  - 选中态：霓虹发光边框 + 数据高亮
  - 进度条：分段式刻度（像音量表）
  - 空状态：HUD 风格十字准星

去除 AI 味改动：
  ✗ 取消一切圆角
  ✗ 取消柔和阴影
  ✓ 直角网格布局
  ✓ 等宽字体数据
  ✓ 扫描线动画
```

### 3.5 黑曜金（Noir Gold）— 藏书家

**设计关键词**：烫金 · 皮革 · 书脊 · 重量

```
色彩氛围：
  底色：黑曜石 #090806 → #080705
  主色：暖金 #d5af62
  辅助：古铜 #8c6a32
  点缀：香槟金 #f2e2b5

间距体系（generous）：
  基础网格：4rpx
  紧凑间距：16rpx
  标准间距：24rpx
  宽阔间距：40rpx

字体体系：
  标题：Songti SC / STSong（宋体，古典感）
  正文：Palatino / "Songti SC"（衬线正文）
  装饰：Georgia（西文衬线）

圆角体系（refined）：
  卡片：10rpx（微圆角，像精装书封）
  按钮：8rpx
  封面：6rpx
  标签：4rpx

特殊 UI 元素：
  - 卡片有金色内框线（像藏书票边框）
  - 卡片有金色角标装饰（四个角的 L 形金线）
  - 选中态：金色描边 + 暖光投影
  - 书架封面有皮革纹理背景（CSS 渐变模拟）
  - 进度条：金色渐变

去除 AI 味改动：
  ✗ 取消发光效果
  ✗ 取消玻璃材质
  ✓ 实色深色面板
  ✓ 金线装饰
  ✓ 宋体标题
  ✓ 藏书票边框
```

---

## 四、全局 UI 组件库重构

### 4.1 按钮系统

```
层级定义：
  L1 - 主操作按钮（CTA）
    → 主题色填充 + 白色文字 + 大尺寸 + 全宽
  L2 - 次要操作按钮
    → 主题色描边 + 透明背景 + 中等尺寸
  L3 - 文字按钮
    → 仅文字 + 下划线 hover + 小尺寸
  L4 - 图标按钮
    → 圆形/方形 + 边框 + 图标居中

交互状态（每个按钮都必须覆盖）：
  rest    → 默认视觉
  hover   → 微亮（+8% 亮度）
  active  → 微暗（-8% 亮度）+ scale(0.97)
  focus   → 外框 outline（键盘导航）
  disabled → 灰度 + 降低透明度 + 不可点击
  loading → 显示 loading spinner + 保持宽度不变

按钮骨架（复用结构）：
  <button class="btn btn--primary btn--lg" :loading="saving">
```

### 4.2 卡片系统

```
变体定义：
  CardList     → 列表项卡片（书架行、书源行）
  CardPanel    → 内容面板卡片（设置项、详情面板）
  CardAction   → 可点击操作卡片（导入方式、主题选择）
  CardStat     → 状态信息卡片（健康检测、导入日志）
  CardEmpty    → 空状态卡片

每种卡片在五个主题下有不同的：
  - border-radius（圆角）
  - border-width（边框粗细）
  - border-color（边框颜色）
  - shadow（投影）
  - 内间距（padding）
  - 背景纹理（pattern）

卡片动画：
  - 列表项入场：translateY(20rpx) + opacity 0 → 1，stagger 60ms
  - 卡片点击：scale(0.985) + 亮度微降
  - 卡片删除：scale(0.9) + opacity 0，200ms
```

### 4.3 输入系统

```
Input 变体：
  SearchPill  → 搜索框（圆角胶囊 + 搜索图标前缀）
  TextField   → 文本输入（底部边框 + 标签浮动）
  TextArea    → 多行输入（卡片样式 + 字数统计）
  Select      → 选择器（下拉箭头 + 选项面板）

交互规范：
  - 聚焦时边框色变为主题色（transition 200ms）
  - 输入中 placeholder 上移为标签（仅限 TextField）
  - 错误状态：红色边框 + 错误信息文本
  - 成功状态：绿色边框 + 对勾图标
```

### 4.4 反馈系统

```
Toast → 顶部/中间浮动提示（2s 自动消失）
  - 成功：绿色图标 + "操作成功"
  - 失败：红色图标 + 错误信息
  - 警告：黄色图标 + 警告信息
  - 加载：spinner + "正在处理..."

Modal → 居中对话框
  - 标题 + 内容 + 1-2 个按钮
  - 背景遮罩 + 卡片弹出动画
  - 点击遮罩可关闭（configurable）

BottomSheet → 底部弹出面板
  - 从底部滑入（300ms cubic-bezier）
  - 顶部有拖拽手柄（横条）
  - 点击遮罩关闭
  - 支持滚动内容

Skeleton → 骨架屏
  - 加载时显示灰色占位块
  - 微光动画（shimmer）
  - 形状匹配内容（圆形→头像/封面，矩形→文字行）
```

---

## 五、页面级改造路线

### 5.1 书架页（Bookshelf）— 优先级 🔴

```
改造要点：
  1. 书籍列表交错入场动画（stagger 60ms）
  2. 封面图加载使用彩色占位块（取主题色的淡色版本）
  3. 左滑删除手势优化：
     - 删除按钮从右侧滑入（跟手）
     - 松手阈值从 56rpx 改为 40% 按钮宽度
     - 删除确认使用底部 Sheet 代替 Modal
  4. 空书架状态：主题化插图替代文字
  5. 下拉刷新（pull-to-refresh）动画
  6. 长按拖拽排序（可选）

去除 AI 味：
  ✗ 取消卡片标准阴影
  ✓ 玄夜：书脊发光竖线
  ✓ 糖果：贴纸封面 + 旋转
  ✓ 樱雾：丝带装饰
  ✓ 量子：网格背景
  ✓ 黑曜：金线边框
```

### 5.2 书源页（Library/Source）— 优先级 🔴

```
改造要点：
  1. 书源列表紧凑化（减少空白，提高信息密度）
  2. 导入 Hub 重新设计：
     - 四个入口（链接/JSON/扫码/文件）改为更大更直观的卡片
     - 每个卡片有主题插图和明确标签
  3. 弹层动画优化：
     - Import drawer 从底部滑入
     - Filter sheet 从右侧滑入
  4. 批量操作进度可视化（进度环 + 实时日志）
  5. 搜索筛选栏吸附顶部（sticky）

去除 AI 味：
  ✗ 取消 emoji 图标（📘☯🌈）
  ✓ 改用 SVG 图标或 CSS 几何图形
  ✓ 书源状态指示灯改为更精致的样式
```

### 5.3 发现/搜索页（Search/Discover）— 优先级 🟡

```
改造要点：
  1. 搜索框重新设计：
     - 更大的搜索区域
     - 搜索图标动画（搜索时旋转）
     - 搜索历史 horizontal scroll chips
  2. 搜索结果卡片：
     - 封面 + 书名 + 作者 + 来源标签
     - 点击变暗反馈
  3. 书源并发搜索进度：
     - 每个书源一个小进度条
     - 实时展示"已搜索 X/Y 个书源"
  4. 分类/发现入口网格重新布局

去除 AI 味：
  ✓ 搜索结果列表使用分割线替代卡片样式
  ✓ 搜索中状态使用骨架屏
```

### 5.4 阅读器页（Reader）— 优先级 🟡

```
改造要点：
  1. 翻页动画优化：
     - 滑动模式：使用 CSS transition + transform
     - 覆盖模式：前一页淡出 + 新页面从右滑入
     - 无动画：即时切换
  2. 控件面板：
     - 顶部/底部栏从边缘滑入（不是淡入）
     - 图标按钮增大触摸区域
  3. 阅读进度条更精致（细线 + 发光）
  4. 章节目录面板从右侧滑入
  5. 设置面板从底部滑入

去除 AI 味：
  ✓ 控件面板减少玻璃效果
  ✓ 进度条主题化（不是统一的渐变色）
```

### 5.5 我的/设置页（Profile）— 优先级 🟢

```
改造要点：
  1. 设置列表分组更清晰（section 间距加大）
  2. 主题选择面板：
     - 预览卡片重新设计（更像真实手机截图）
     - 当前选中主题有明确勾选标记
     - 切换主题有过渡动画（不是即时切换）
  3. 后端连接卡片简化
  4. 调试模式面板折叠（默认隐藏）

去除 AI 味：
  ✗ 取消复杂的调试面板 UI
  ✓ 设置页面保持简约
  ✓ 主题卡片更真实
```

### 5.6 新增：过渡与转场动画 — 优先级 🟡

```
页面间转场：
  push（进入）: 新页面从右滑入（300ms，cubic-bezier(0.4, 0, 0.2, 1)）
  pop（返回）: 当前页面向右滑出（250ms）
  tab切换: 无滑动，仅内容区内容淡入（200ms）

共享元素过渡（可选，优先级低）：
  书架 → 阅读器：封面图平滑过渡
  书源列表 → 书源详情：书源名称平滑过渡
```

---

## 六、性能专项优化

### 6.1 渲染性能

| 优化项 | 方案 | 预期收益 |
|--------|------|---------|
| 长列表虚拟化 | 书架 > 50 本时启用虚拟列表（recycle-list） | 减少 DOM 节点 80%+ |
| 图片懒加载 | 所有封面图 lazy-load + 占位色块 | 首屏加载快 40% |
| CSS 动画 GPU 加速 | 仅使用 transform + opacity 做动画 | 避免重排（reflow） |
| backdrop-filter 降级 | 低端设备自动降级为实色背景 | 帧率 60fps 保证 |
| 防抖输入 | 搜索输入 debounce 300ms | 减少无效计算 |

### 6.2 响应速度

| 优化项 | 方案 | 目标 |
|--------|------|------|
| Tab 切换 | 预加载相邻 Tab 页面（uni.preloadPage） | < 16ms |
| 按钮反馈 | CSS :active 即时视觉反馈 | < 50ms |
| 页面加载 | 骨架屏 + 渐进式内容填充 | 首屏 < 500ms |
| 主题切换 | CSS 变量即时生效 + 页面背景过渡 | < 100ms |

### 6.3 包体积

| 优化项 | 方案 | 预期 |
|--------|------|------|
| 图标系统 | iconfont 替代 PNG 图标 | 减少 ~80KB |
| CSS 去重 | 提取公共样式到全局 App.vue | 减少 ~30KB |
| 代码分割 | uni-app 自动分包（pages.json subPackages） | 主包 < 500KB |

---

## 七、实施优先级与排期

### 🔴 P0 — 核心体验（第 1-2 周）

| 任务 | 预估工时 | 说明 |
|------|---------|------|
| 底部导航栏重构 | 3 天 | 动画系统 + 图标系统 + 手势支持 |
| 全局按钮/卡片/输入组件 | 2 天 | 建立组件变体体系 |
| 书架页改造 | 3 天 | 交错动画 + 左滑优化 + 空状态 |
| 主题 Token 细化 | 2 天 | 间距/字体/圆角 token 补全 |

### 🟡 P1 — 主题深化（第 3-4 周）

| 任务 | 预估工时 | 说明 |
|------|---------|------|
| 玄夜主题完整落地 | 1.5 天 | 所有页面 + 特殊元素 |
| 糖果绘本主题完整落地 | 1.5 天 | 贴纸/印章/手账风格 |
| 樱雾少女主题完整落地 | 1.5 天 | 丝带/薄雾/花卉风格 |
| 量子蓝图主题完整落地 | 1.5 天 | 网格/扫描线/HUD 风格 |
| 黑曜金主题完整落地 | 1.5 天 | 烫金/皮革/书脊风格 |
| 书源页改造 | 2 天 | 导入 Hub + 弹层 + 批量操作 |

### 🟢 P2 — 体验打磨（第 5-6 周）

| 任务 | 预估工时 | 说明 |
|------|---------|------|
| 阅读器页改造 | 2 天 | 翻页动画 + 控件面板 |
| 发现/搜索页改造 | 2 天 | 搜索框 + 结果列表 + 进度 |
| 设置页改造 | 1 天 | 布局简化 + 主题面板 |
| 页面转场动画 | 1 天 | push/pop/tab 转场 |
| 骨架屏系统 | 1 天 | 各页面加载状态 |
| 性能优化收口 | 2 天 | 虚拟列表 + 图片 + 动画性能 |

---

## 八、开发约束（与 Codex 协作）

### 8.1 不做的事情

- 不迁移框架（保持 uni-app + Vue 2）
- 不重写后端接口
- 不改变现有页面路由结构
- 不修改书源解析/阅读进度/缓存逻辑
- 不引入新的 npm 依赖（除非绝对必要且轻量）

### 8.2 实施原则

1. **渐进改造**：每次只改一个页面，保证可运行
2. **向后兼容**：不破坏现有书源导入/搜索/阅读功能
3. **主题先行**：先在 1 个主题（玄夜）做完整改造，验证后再复制到其他主题
4. **每步验收**：改完一个页面 → H5 验证 → Android 验证 → 再改下一个
5. **CSS Token 驱动**：所有视觉变化通过 `--app-*` Token 控制，不写死颜色

### 8.3 Token 体系扩展

当前已有 Token 需要细化：

```css
/* 新增间距 Token */
--app-space-xs: 8rpx;
--app-space-sm: 16rpx;
--app-space-md: 24rpx;
--app-space-lg: 32rpx;
--app-space-xl: 48rpx;

/* 新增字体 Token */
--app-font-size-xs: 20rpx;   /* 标签/辅助文字 */
--app-font-size-sm: 24rpx;   /* 次要正文 */
--app-font-size-md: 28rpx;   /* 正文 */
--app-font-size-lg: 36rpx;   /* 小标题 */
--app-font-size-xl: 44rpx;   /* 大标题 */
--app-font-size-xxl: 56rpx;  /* 页面标题 */

/* 新增动效 Token */
--app-motion-duration-fast: 120ms;
--app-motion-duration-normal: 240ms;
--app-motion-duration-slow: 380ms;
--app-motion-spring: cubic-bezier(0.34, 1.56, 0.64, 1);
--app-motion-smooth: cubic-bezier(0.4, 0, 0.2, 1);
--app-motion-linear: cubic-bezier(0, 0, 1, 1);

/* 新增层级 Token */
--app-z-base: 0;
--app-z-dropdown: 100;
--app-z-sticky: 200;
--app-z-overlay: 300;
--app-z-modal: 400;
--app-z-toast: 500;

/* 新增触摸 Token */
--app-touch-target-min: 88rpx;  /* 最小触摸区域 */
--app-touch-feedback-scale: 0.97;
--app-touch-feedback-duration: 80ms;
```

---

## 九、验收标准

### 9.1 视觉验收

- [ ] 五个主题各截取书架/书源/发现/我的/阅读器 5 个页面共 25 张截图
- [ ] 每个主题的截图放在一起能明显看出设计风格差异
- [ ] 没有玻璃拟物 + 紫色渐变 + 等宽字体 + 圆角卡片的 AI 四件套痕迹
- [ ] 375px / 393px / 412px 三个视口无溢出、不错位

### 9.2 动效验收

- [ ] Tab 切换：指示器弹性滑动 + 图标变形（录屏 30fps 无掉帧）
- [ ] 按钮点击：触碰瞬间有视觉反馈（50ms 内可见）
- [ ] 列表滚动：60fps 无卡顿（Chrome DevTools Performance 面板确认）
- [ ] 页面转场：流畅无白屏

### 9.3 性能验收

- [ ] Lighthouse Performance 评分 > 85
- [ ] 首屏加载 < 1s（H5 模式）
- [ ] Tab 切换延迟 < 50ms
- [ ] 主题切换延迟 < 100ms
- [ ] Android 真机（荣耀 REA-AN00）60fps 滚动

### 9.4 功能回归

- [ ] 书源导入（URL/JSON/扫码/文件）正常
- [ ] 搜索 → 详情 → 目录 → 正文 → 加入书架 全链路正常
- [ ] 书架左滑删除正常
- [ ] 主题切换持久化正常
- [ ] 静态回归测试全部通过

---

## 十、参考灵感

| 来源 | 借鉴方向 |
|------|---------|
| Apple Books | 书架布局 + 翻页动画 + 阅读器控件 |
| Kindle App | 阅读器排版 + 主题色温调节 |
| Notion | 简洁 UI + 拖拽排序 |
| Linear | 暗色主题 + 键盘快捷键 + 动效 |
| 微信读书 | 书架卡片 + 阅读进度展示 |
| Spotify | 底部导航 + 主题切换 + 专辑封面 |
| Things 3 | 简洁白 + 细线 + 优雅动效 |
| Reflect | 暗色笔记 + 衬线字体 + 留白 |

---

> **这份计划的核心理念**：不要让人一眼看出"这是 AI 生成的界面"。
> 要做到：每个主题像一位有品味的设计师花了一周打磨出来的、有温度、有细节、有性格的作品。
> 少即是多，克制是最高级的表达。
